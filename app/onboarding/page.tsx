"use client";import{useState}from"react";import"../pages.css";const steps=["The basics","Brand & appearance","Your menu","Ready to launch"];
export default function Onboarding(){const[step,setStep]=useState(0),[name,setName]=useState("Saffron Table"),[theme,setTheme]=useState("saffron"),[saving,setSaving]=useState(false);async function next(){if(step<3){setStep(step+1);return}setSaving(true);const r=await fetch("/api/restaurants",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({name,slug:name.toLowerCase().replace(/[^a-z0-9]+/g,"-").replace(/(^-|-$)/g,""),description:"Contemporary Indian cooking, made with warmth and a little bit of spice.",theme})});if(r.ok)location.assign("/dashboard");else setSaving(false)}return <main className="onboardingPage"><section><aside><p className="eyebrow">RESTAURANT LAUNCHPAD</p><h1>Let’s put your place on the map.</h1><p>Answer a few questions and we’ll create the first version of your ordering site.</p><ol>{steps.map((x,i)=><li className={i===step?"active":i<step?"done":""} key={x}><b>0{i+1}</b>{x}</li>)}</ol></aside><form onSubmit={e=>{e.preventDefault();next()}}><p className="eyebrow">STEP {step+1} OF 4</p><div className="stepMeter"><i style={{width:`${(step+1)*25}%`}}/></div>{step===0&&<><h2>Tell us about your place</h2><label>Restaurant name<input value={name} onChange={e=>setName(e.target.value)}/></label></>}{step===1&&<><h2>Make it feel like you.</h2><div className="themeChoices"><button className={theme==="saffron"?"selected":""} onClick={()=>setTheme("saffron")} type="button">Saffron <small>Warm & expressive</small></button><button className={theme==="olive"?"selected oliveChoice":"oliveChoice"} onClick={()=>setTheme("olive")} type="button">Olive <small>Calm, seasonal & considered</small></button></div></>}{step===2&&<><h2>Add your first dish.</h2><label>Signature dish<input defaultValue="Butter Chicken"/></label></>}{step===3&&<div className="launchStep"><span>{theme==="olive"?"❋":"✦"}</span><h2>{name} is ready for its close-up.</h2><p>Your {theme} storefront will be created locally.</p></div>}<div className="onboardActions"><span/>{step>0&&<button type="button" className="textButton" onClick={()=>setStep(step-1)}>← Back</button>}<button className="button" disabled={saving}>{saving?"Creating…":step===3?"Open my dashboard →":"Continue →"}</button></div></form></section></main>}
